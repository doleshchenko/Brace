namespace Brace.Commands
{
    public enum CommandType
    {
        Unknown,
        Void,
        GetContent,
        Enumerate,
        AddDocument,
        UpdateDocument,
        DeleteDocument
    }
}
